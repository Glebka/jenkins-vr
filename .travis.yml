language: cpp
compiler:
  - gcc
  - clang
before_install:
  - sudo apt-get update -qq
install:
  - sudo apt-get install make cmake gstreamer1.0-x gstreamer1.0-plugins-good gstreamer1.0-plugins-bad libgstreamer1.0-dev
before_script:
  - cmake .
script: make